[project]
name = "micrograd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "graphviz>=0.21",
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
]

[tool.uv.workspace]
members = [
    "packages/micrograd",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
